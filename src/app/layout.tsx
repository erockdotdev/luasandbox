import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lua Sandbox App",
  description:
    "A simple app for Eric's Lua technical interview, built with Next.js and TypeScript. The app is designed to be simple and easy to use, with a clean and modern UI. It also serves as a demonstration of my ability to build a modern web application using Next.js and TypeScript, as well as my understanding of web development best practices and design patterns.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
